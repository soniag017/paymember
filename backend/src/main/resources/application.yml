server:
  port: 8080

spring:
  profiles:
    active: mysql
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

app:
  jwt:
    secret: change-this-secret-in-production-paymember-2026
    expiration-ms: 86400000
  google:
    web-client-id: replace-with-your-web-client-id.apps.googleusercontent.com
